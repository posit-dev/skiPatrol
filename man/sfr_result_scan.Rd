% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{sfr_result_scan}
\alias{sfr_result_scan}
\title{Retrieve a previous query's results via RESULT_SCAN}
\usage{
sfr_result_scan(
  conn,
  query_id = NULL,
  cell = NULL,
  dataframe = NULL,
  .keep_case = !getOption("skiPatrol.lowercase_columns", FALSE)
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{query_id}{Character. An explicit Snowflake query ID.
If \code{NULL}, looks up the ID via \code{cell} or \code{dataframe}.}

\item{cell}{Integer. The IPython cell execution count (\code{In[N]}).}

\item{dataframe}{Character. The Workspace \code{dataframe_N} variable name.}

\item{.keep_case}{Logical. If \code{TRUE} (default), preserve original column
name casing from Snowflake.}
}
\value{
A data.frame with the query results.
}
\description{
Executes \verb{SELECT * FROM TABLE(RESULT_SCAN('<query_id>'))} against
Snowflake.  The query ID can be provided explicitly, or looked up
from the notebook query tracker by cell execution count or
\code{dataframe_N} name.
}
\details{
Requires the query tracker to be installed (automatic when using
\code{setup_notebook()} in a Workspace Notebook).
}
\examples{
\dontrun{
conn <- sfr_connect()

# Explicit query ID
df <- sfr_result_scan(conn, query_id = "01c34f1a-0814-fa4a-0000-0c09644ace8a")

# By dataframe name (from SQL cell)
df <- sfr_result_scan(conn, dataframe = "dataframe_4")

# By cell number
df <- sfr_result_scan(conn, cell = 3)

# Most recent user query
df <- sfr_result_scan(conn)
}

}
