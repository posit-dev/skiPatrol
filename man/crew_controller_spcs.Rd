% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crew_spcs.R
\name{crew_controller_spcs}
\alias{crew_controller_spcs}
\title{Create a crew Controller with SPCS Workers}
\usage{
crew_controller_spcs(
  conn,
  compute_pool,
  image,
  workers = 4L,
  seconds_idle = 120,
  host = "auto",
  port = 5555L,
  instance_family = "CPU_X64_S",
  eai = ""
)
}
\arguments{
\item{conn}{An \code{sfr_connection} object from \code{\link[=sfr_connect]{sfr_connect()}}.}

\item{compute_pool}{Character. SPCS compute pool for workers.}

\item{image}{Character. Docker image URI with R + crew + mirai
pre-installed.}

\item{workers}{Integer. Maximum number of concurrent workers (default 4).}

\item{seconds_idle}{Numeric. Seconds before idle workers are terminated
(default 120).}

\item{host}{Character. Controller hostname. If \code{"auto"}, determines
the correct host based on the execution environment.}

\item{port}{Integer. Controller listener port (default 5555).}

\item{instance_family}{Character. SPCS instance family (default
\code{"CPU_X64_S"}).}

\item{eai}{Character. External Access Integration name.}
}
\value{
A \code{crew_controller} object.
}
\description{
Convenience function that creates a \code{crew} controller configured to
launch workers on Snowflake SPCS. Workers are launched as ephemeral
EXECUTE JOB SERVICE instances that connect back to the controller
via TCP.
}
\examples{
\dontrun{
library(crew)
library(skiPatrol)

conn <- sfr_connect()
controller <- crew_controller_spcs(
  conn, compute_pool = "MY_POOL",
  image = "/DB/SCHEMA/REPO/r_crew:latest",
  workers = 8
)
controller$start()

# Submit tasks
controller$push(quote(1 + 1))
controller$pop()

controller$terminate()
}
}
