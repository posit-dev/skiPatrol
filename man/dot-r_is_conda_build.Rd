% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{.r_is_conda_build}
\alias{.r_is_conda_build}
\title{Detect whether the running R was installed via conda/mamba/micromamba}
\usage{
.r_is_conda_build(
  home = R.home(),
  conda_prefix = Sys.getenv("CONDA_PREFIX", "")
)
}
\arguments{
\item{home}{\code{R.home()}, injectable for testing.}

\item{conda_prefix}{\code{Sys.getenv("CONDA_PREFIX")}, injectable for testing.}
}
\value{
TRUE/FALSE.
}
\description{
\code{r-base} is conda-forge's package name for the R interpreter; it only
means anything inside a conda/mamba environment. Most R installs --
CRAN, Posit's own builds, system packages, what most customers running
this package will have -- are not conda builds and have no \code{r-base}
identity at all. Workspace Notebooks are the deliberate exception (via
\code{sfnb_multilang}): conda/micromamba was the only way to install R
packages there without root, and separately the Model Registry's SPCS
inference container is \emph{always} built via conda regardless of what
produced the training R. \code{.pin_r_versions()} uses this to decide
whether pinning \code{r-base} to \emph{this} R's exact version is meaningful: it
only is if this R itself came from that channel. See DB-8 and
\code{posit_native_app_support/native-app-probe-findings.md}'s "Incidental
confirmation of DB-8", which used the same check by hand
(\verb{command -v R} containing "conda"/"mamba") against a live Native App.
}
\keyword{internal}
