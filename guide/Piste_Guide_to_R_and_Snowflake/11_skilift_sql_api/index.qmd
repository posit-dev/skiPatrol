---
title: "The Snowflake SQL API"
subtitle: "Driverless REST access — how skiLift connects"
---

## Overview

**skiLift** does not use JDBC or ODBC. It speaks to Snowflake through the **SQL API** — HTTPS REST endpoints that submit SQL and return results as JSON (or Arrow with **ADBC** for bulk I/O).

::: {.callout-note title="What is ADBC?"}
**ADBC** (Arrow Database Connectivity) is a **columnar database access standard** built on Apache Arrow — not a Snowflake-specific product. skiLift can route large result sets through the Snowflake **ADBC driver** (`adbcsnowflake`) instead of deserializing JSON from the SQL API. See [Glossary](../appendices/F_glossary/index.qmd) and [skiLift: Query — Arrow](../13_skilift_query/index.qmd#sec-arrow).
:::

Understanding the SQL API explains skiLift's strengths, limits, and why patterns like **WebR-in-the-browser** are possible without a native driver install.

## Learning Objectives

- Explain what the SQL API is and how it differs from classic drivers
- List supported vs unsupported command classes
- Compare SQL API vs installed drivers (pros/cons)
- See how Arrow/ADBC improves bulk reads

Terms: [Glossary](../appendices/F_glossary/index.qmd).

---

## What is the SQL API? {#sec-what}

The [Snowflake SQL API](https://docs.snowflake.com/en/developer-guide/sql-api/index) lets any HTTP client:

1. Authenticate (JWT, OAuth, PAT, etc.)
2. `POST` a SQL statement to `/api/v2/statements`
3. Poll for completion and fetch result sets

**No driver binary** — only TLS and JSON. skiLift implements DBI on top of this stack.

```{mermaid}
sequenceDiagram
  participant R as R skiLift
  participant API as SQL REST API
  participant SF as Snowflake engine
  R->>API: POST statement plus auth
  API->>SF: Execute SQL
  SF-->>API: Result partitions
  API-->>R: JSON or Arrow rows
```

## What it supports {#sec-supports}

Typical **server-side SQL** works:

- `SELECT`, `INSERT`, `MERGE`, DDL (`CREATE TABLE`, etc.)
- Most DML/DDL you would run from a SQL worksheet
- Parameter binding (with skiLift's DBI interface)
- Multi-statement batches (within API limits)

## What it does not support {#sec-limits}

Client-side or session-protocol commands are **not** available over the SQL API:

| Unsupported | Implication |
|-------------|-------------|
| `GET` / `PUT` | No stage file transfer via SQL — use **stage volume mounts** in SPCS or Snowflake's file API elsewhere |
| `USE DATABASE` / `USE SCHEMA` | Set context in connection params or fully qualify names |
| `ALTER SESSION` | Set session parameters via supported alternatives or JDBC-only paths |
| Some legacy commands | See Snowflake SQL API docs for current list |

::: {.callout-warning}
SPCS workers using skiLift **must** use volume-mounted stages for file I/O — not `GET`/`PUT` SQL. See [Troubleshooting](../appendices/C_troubleshooting/index.qmd).
:::

## Bulk inserts vs driver file loads {#sec-bulk-inserts}

The SQL API supports **parameter binding** and batched `INSERT` statements — skiLift uses this path when ADBC and Snowpark fallbacks are unavailable. That works for moderate row counts, but it is **not** the fastest way to land millions of rows:

| Path | Transport | Typical use |
|------|-----------|-------------|
| **Bind-based INSERT** (SQL API) | One HTTP round-trip per batch; JSON serialization per row | Small/medium uploads, ad hoc `dbAppendTable()` |
| **ADBC bulk upload** (skiLift) | Columnar Arrow stream | Large in-account writes without stage files |
| **PUT + COPY** (JDBC/ODBC driver) | Stage file upload + server-side `COPY INTO` | **Highest throughput** for very large local files |

::: {.callout-important}
If you are moving a **large volume of data** into Snowflake from R, prefer a **driver-based** workflow (**JDBC/ODBC `PUT` + `COPY`**, or skiLift **ADBC** bulk upload when installed) over SQL API bind inserts. skiLift's `upload_method = "auto"` already prefers ADBC when available — see [Query — Bulk write routing](../13_skilift_query/index.qmd#sec-bulk). For terabyte-scale file loads outside DBI, classic drivers remain the standard pattern.
:::

## Pros and cons {#sec-pros-cons}

| | SQL API (skiLift) | JDBC/ODBC driver |
|--|----------------------|------------------|
| **Install footprint** | R package only | JVM/ODBC stack, admin burden |
| **Serverless / browser** | Possible (HTTPS) | Impractical |
| **DBI + dbplyr** | Yes | Yes (other drivers) |
| **Stage file I/O** | No `GET`/`PUT` | Often yes |
| **Ecosystem maturity** | Newer | Decades of tooling (incl. Posit [`odbc`](https://odbc.r-dbi.org/) + Snowflake ODBC — see [skiLift vs ODBC](../04_rstudio_posit_vscode/index.qmd#sec-odbc)) |

### Why WebR + SQL API matters {#sec-webr}

[WebR](https://docs.r-wasm.org/webr/latest/) runs R in the browser (WASM). Without a native Snowflake driver, **skiLift + SQL API** is a viable path to query Snowflake from client-only R — useful for demos, internal tools, or constrained desktops. Production patterns still favor full RStudio/Posit or Workspace for ML workflows.

## Arrow and ADBC {#sec-arrow}

Default SQL API results deserialize through JSON — fine for moderate row counts.

For **large bulk reads**, skiLift optionally uses **ADBC** — the Arrow-native driver path described in the callout above:

- Columnar Arrow streams with less overhead
- Automatic fallback to SQL API if ADBC unavailable
- Workspace bootstrap may need extra install time for ADBC (Go driver compile)

See [skiLift: Query](../13_skilift_query/index.qmd#sec-arrow).

## Relationship to skiPatrol {#sec-skipatrol}

| Package | Transport |
|---------|-----------|
| **skiLift** | SQL API (+ optional ADBC) |
| **skiPatrol** | Python `snowflake-ml-python` / Snowpark session (different stack) |

Use `sfr_dbi_connection()` when you need both in one R session.

## Next steps {#sec-next}

[skiLift: Connect](../12_skilift_connect/index.qmd) — auth and `dbConnect()`.

[skiLift: Query](../13_skilift_query/index.qmd) — dbplyr and bulk I/O.
